#take the user input a,b and perform sum,sub,mul,div

a=int(input("Type a number"))
b=int(input("Type another number"))
print("sum is:",a+b)
print("sub is:",a-b)
print("mul is:",a*b)
print("div is:",a/b)

# it will not perform the mathmatical operation because it is str data type,
# first we need to convert into int
